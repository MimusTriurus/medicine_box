version: '3.3'

services:
  bot:
    container_name: tg_bot
    build: .
    volumes:
      - ./bot/logs/:/app/bot/logs
    environment:
      WEBAPP_PORT: 8000
    env_file:
      - ./vault.env
    network_mode: host